import { LogoWrapper, RotatingIcon } from './Logo.styled';
export const Logo = () => {
  return (
    <header>
      <LogoWrapper>
        <RotatingIcon size={42} color="#61DAFB" />
      </LogoWrapper>
    </header>
  );
};
