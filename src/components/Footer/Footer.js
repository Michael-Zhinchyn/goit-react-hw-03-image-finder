import { FooterWrapper, RotatingIcon } from './Footer.styled';
export const Footer = () => {
  return (
    <FooterWrapper>
      <RotatingIcon size={50} color="#61DAFB" />
    </FooterWrapper>
  );
};
